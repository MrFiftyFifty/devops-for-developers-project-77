[webservers]
%{ for i, server in web_servers ~}
${server.name} ansible_host=${server.network_interface[0].nat_ip_address}
%{ endfor ~}

[webservers:vars]
ansible_user=ubuntu
ansible_ssh_private_key_file=~/.ssh/id_rsa
